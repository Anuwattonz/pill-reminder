# ========================================================
# 🌍 APPLICATION SETTINGS
# ========================================================
APP_ENV=production                 # development / staging / production
API_BASE_PATH=/pill-reminder/api                 # พาธ API ของระบบ เช่น /api หรือ /pill-reminder/api

# ========================================================
# 🗄️ DATABASE CONNECTION
# ========================================================
DB_HOST=127.0.0.1                  # Host ของฐานข้อมูล เช่น 127.0.0.1 หรือ db.example.com
DB_NAME=pill_reminder_db           # ชื่อฐานข้อมูล
DB_USER=admin                       # ชื่อผู้ใช้ฐานข้อมูล
DB_PASS=strongpassword123           # รหัสผ่านฐานข้อมูล (ควรปลอดภัย)
DB_CHARSET=utf8mb4                 # Charset ของฐานข้อมูล
DB_TIMEZONE=+07:00                 # Timezone ของฐานข้อมูล

# ========================================================
# 🔐 JWT AUTHENTICATION
# ========================================================
JWT_SECRET=your_64_char_random_key # รหัสลับสำหรับ JWT (64 ตัวอักษรขึ้นไป)
JWT_EXPIRATION=1000                 # เวลา JWT หมดอายุ (วินาที)
REFRESH_EXPIRATION=604800           # เวลา Refresh Token หมดอายุ (วินาที)
JWT_LEEWAY=100                       # เวลาเผื่อหน่วยวินาที
JWT_ISSUER=pill-reminder-system     # ผู้สร้าง JWT
JWT_AUDIENCE=pill-reminder-app      # ผู้รับ JWT

# ========================================================
# 📁 FILE UPLOAD SYSTEM
# ========================================================
UPLOAD_BASE_URL=http://example.com/pill-reminder/uploads/  # URL สำหรับเข้าถึงไฟล์อัปโหลด
UPLOAD_DIR_PATH=../uploads/                              # พาธบนเซิร์ฟเวอร์เก็บไฟล์
MAX_UPLOAD_SIZE=2097152                                   # ขนาดไฟล์สูงสุด (2MB)
MAX_IMAGE_WIDTH=1920                                      # ขนาดภาพสูงสุด (พิกเซล)
MAX_IMAGE_HEIGHT=1080                                     # ขนาดภาพสูงสุด (พิกเซล)
ALLOWED_EXTENSIONS=jpg,jpeg,png                           # นามสกุลไฟล์ที่อนุญาต
ALLOWED_MIME_TYPES=image/jpeg,image/png                  # MIME Type ของไฟล์ที่อนุญาต

# ========================================================
# 📧 EMAIL & SMTP SETTINGS
# ========================================================
EMAIL_ENABLED=true                 # true/false เปิด/ปิดระบบส่งอีเมล
SMTP_HOST=smtp.gmail.com           # Host SMTP เช่น smtp.gmail.com
SMTP_PORT=587                      # Port SMTP 587 (TLS) / 465 (SSL)
SMTP_USERNAME=your_email@gmail.com # อีเมลผู้ส่ง
SMTP_PASSWORD=your_app_password    # App Password จาก email
SMTP_ENCRYPTION=tls                # tls / ssl
SMTP_FROM_EMAIL=your_email@gmail.com
SMTP_FROM_NAME=Pill Reminder System
EMAIL_TIMEOUT=30                    # เวลา timeout (วินาที)
EMAIL_DEBUG=false                   # true/false เปิด debug

# ========================================================
# 🔢 OTP (One-Time Password) SYSTEM
# ========================================================
OTP_ENABLED=true                    # true/false เปิดระบบ OTP
OTP_LENGTH=6                         # จำนวนตัวเลข OTP
OTP_EXPIRY_MINUTES=4                 # หมดอายุ OTP (นาที)
OTP_EMAIL_ENABLED=true               # ส่ง OTP ทางอีเมล
OTP_RATE_LIMIT_MINUTES=1             # เวลาห้ามขอ OTP ซ้ำ (นาที)
OTP_MAX_ATTEMPTS=2                   # จำนวนครั้งสูงสุดที่กรอก OTP ผิด

# ========================================================
# 🌐 CORS & SECURITY
# ========================================================
ALLOWED_ORIGINS=*                    # URL ที่อนุญาตเรียก API เช่น https://yourdomain.com
CORS_ALLOW_CREDENTIALS=true          # true/false อนุญาตส่ง cookie/credentials
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=Content-Type,Authorization

# ========================================================
# ⚙️ SYSTEM DEFAULT VALUES
# ========================================================
DEFAULT_VOLUME=100                    # ระดับเสียงเริ่มต้น (0-100)
DEFAULT_DELAY=00:03:00                # เวลาล่าช้าก่อนแจ้งเตือน (hh:mm:ss)
DEFAULT_ALERT_OFFSET=00:00:30         # เวลาล่วงหน้าก่อนแจ้งเตือน (hh:mm:ss)
DEFAULT_APP_STATUS=0                  # สถานะเริ่มต้น 0=ปิด, 1=เปิด

